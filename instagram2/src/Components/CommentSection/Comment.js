import React from 'react';


function Commment(props){
    return(
        <>
        hiii
        {/* {props.comment.map(post => (
          <div key={comment.username} className="commentsContainer">
           <div>{comment.username}</div>
          </div>
        ))} */}
      </>
    )
}

export default Commment;